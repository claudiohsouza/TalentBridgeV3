{
  "rewrites": [
    { 
      "source": "/api/(.*)", 
      "destination": "${BACKEND_URL}/api/$1" 
    },
    { 
      "source": "/(.*)", 
      "destination": "/index.html" 
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "build"
} 